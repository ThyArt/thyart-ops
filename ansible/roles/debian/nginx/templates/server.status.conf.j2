status {
       server {
       listen 81;
       server_name localhost;
       status_zone par1;

       access_log off;
       allow 127.0.0.1;
       deny all;

       location /nginx_status {
           stub_status;
	  }
	}
}